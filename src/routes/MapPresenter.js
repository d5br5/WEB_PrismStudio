import React from "react";
import styled from "styled-components";

const Container = styled.div`
  width: 100%;
`;

const MapPresenter = ({shopList}) => {
    return <Container>
        s
    </Container>;
}

export default MapPresenter;